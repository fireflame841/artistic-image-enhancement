to run the code the following steps need to be followed:
you should use the given command line argumnet :
python part2.py input_image.jpg

the median cut image is saved with name "median_cut_image"
the floyd steinberg image is saved with name "floyd_steinberg_image"
you can adjust the parameters manually in the code for the respective functions
note:the floyd dithering function will only work after median cut is called